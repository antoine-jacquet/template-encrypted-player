GitHub Actions stores this encrypted payload in the repository and decrypts it on
the runner just before calling the strategy script. Keep it committed so the
server-triggered workflow always has a private strategy to submit.